Context Synthesizer — direct install package
=============================================

No git. No GitHub. No rclone. No separate API key at setup (Claude Code Max/Pro login forwards auth).

TEAM LEAD (once)
----------------
1. Build: bash context-synthesizer/packaging/build-release-tarball.sh
2. Upload to SharePoint (extracted folder recommended, or .tar.gz)
3. Edit team.conf → set SYNC_DIR to your synced OneDrive weekly folder
   (ENABLE_PROXY=1 and ENABLE_WEEKLY_CRON=1 are on by default)
4. Share the SharePoint link; tell team to click Sync in OneDrive

UBUNTU / WSL
------------
  sudo apt install -y python3 python3-venv   # if needed
  Same command: bash run-setup.sh firstname.lastname

  WSL — enable systemd once (/etc/wsl.conf → [boot] systemd=true, then wsl --shutdown)
  WSL + Windows browser: bash context-synthesizer/scripts/open_dashboard.sh
        (WSL IP + ?token=... — not 127.0.0.1 in Chrome)

DEVELOPER (once, ~5 min)
------------------------
1. Get the package:
     Windows — SharePoint link → Sync (OneDrive desktop app)
     Ubuntu  — OneDrive Linux client sync, OR download .tar.gz from SharePoint and extract
2. Open terminal in the package folder, e.g.:
     .../ContextSynthesizer/context-synthesizer-toolkit-2026.06.16/
3. Run (use your Azure email local-part as id):

     bash run-setup.sh firstname.lastname

   Example:
     bash run-setup.sh harshil.shah

4. Done. Live compaction + dashboard run in the background.

SMOKE TEST
----------
  systemctl --user status context-synthesizer-proxy
  bash context-synthesizer/scripts/check_proxy_ready.sh
  bash context-synthesizer/scripts/open_dashboard.sh
  bash context-synthesizer/scripts/open_dashboard.sh --open   # WSL → Windows browser
  # WSL: URLs include ?token=... — use open_dashboard.sh output, do not bookmark bare /dashboard
  # use Claude Code — dashboard charts update per API call

Optional weekly upload test:
  bash context-synthesizer/scripts/weekly_sync.sh

WHAT YOU GET
------------
PRIMARY — Live compaction (default ON, ENABLE_PROXY=1):
- Claude Code routes through local proxy (systemd user service)
- Dreaming v4 compacts history during long sessions (~90-99% smaller shaped context)
- Use Claude Code while logged in to Max/Pro — CLI forwards auth; no extra API key at setup

LIVE DASHBOARD — bash context-synthesizer/scripts/open_dashboard.sh:
- Billing bifurcation: cache read / cache write / uncached tokens per turn
- Four-layer payload: L1 rules, L2 ledger, L3 recent, L4 prompt
- Naive IDE history vs shaped context (where compaction saves size)
- Cumulative $ saved, compaction timeline, tuning recommendations
- WSL: open WSL IP URL in Windows browser (see docs/guides/DASHBOARD.md)
- WSL: DASHBOARD_TOKEN auto-set in context-synthesizer/.env — open via open_dashboard.sh only

OPTIONAL — Weekly SharePoint reports (ENABLE_WEEKLY_CRON=1 in team.conf):
- Automatic summary.md + JSONL every Monday for team rollup

SUPPORT
-------
  docs/guides/DEVELOPER_ONBOARDING.md  — install & smoke test
  docs/guides/DASHBOARD.md             — live metrics UI (incl. WSL + token)
  docs/guides/DEPLOY.md                — team lead rollup
