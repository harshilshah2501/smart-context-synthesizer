# Motadata SharePoint / OneDrive sync root (edit once if your Windows username changes).
# Team lead: bash packaging/publish-to-sharepoint.sh
# Developers: download/sync this folder from SharePoint, then bash run-setup.sh <id>

SHARE_DIR_WIN='C:\Users\Harshil Shah\OneDrive - Motadata\Context-Synthesizer'
SHARE_DIR_WSL='/mnt/c/Users/Harshil Shah/OneDrive - Motadata/Context-Synthesizer'

# Stable folder name — always points at the latest publish (no version in dev commands).
LATEST_NAME='context-synthesizer-toolkit-latest'
