# packaging/ — deprecated

**Not used for team rollout.** The project uses offline **Modes A / C / D** (session corpus import). No `.deb`, no proxy.

| Path | Status |
|------|--------|
| `build-deb.sh`, `DEBIAN/`, `*.service` | Legacy — unmaintained |
| `UBUNTU_INSTALL.md` | Historical reference only |

**Current delivery:** Python scripts in `context-synthesizer/` run from the repo venv. See [Usage.md](../context-synthesizer/Usage.md).

Team workflow uses offline Modes A / C / D only — see [Usage.md](../context-synthesizer/Usage.md).
